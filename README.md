Contains code and scripts used to serve Markdown notes

- **Renderer**: NodeJS scripts used to render Markdown files to HTML
- **Server**: Rust webserver (using Rocket), to search and view rendered notes